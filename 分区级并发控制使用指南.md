# Hudi 分区级并发控制使用指南

## 概述

本文档介绍如何使用 Hudi 的分区级并发控制机制，实现 Flink 和 Spark 同时并发写入不同分区而不会产生冲突。

## 背景

Hudi 默认的 OCC（乐观并发控制）机制是基于**文件组级别**的冲突检测，即两个并发写入者更新同一个文件组时会被检测为冲突。这意味着即使两个写入者写入不同的分区，如果它们写入相同的文件ID，仍然会被判定为冲突。

在某些场景下，我们希望实现**分区级别的并发控制**：
- Flink 写入分区 A
- Spark 写入分区 B
- 两者可以完全并发执行，不会产生冲突

## 实现方案

我们提供了两个新的策略类来实现分区级并发控制：

### 1. 提交时冲突检测策略

**类名**：`PartitionBasedConcurrentWritesConflictResolutionStrategy`

**位置**：`org.apache.hudi.client.transaction.PartitionBasedConcurrentWritesConflictResolutionStrategy`

**功能**：在提交阶段进行分区级冲突检测，只检查分区路径，不检查文件ID。

### 2. 早期冲突检测策略

**类名**：`PartitionBasedDirectMarkerDetectionStrategy`

**位置**：`org.apache.hudi.table.marker.PartitionBasedDirectMarkerDetectionStrategy`

**功能**：在写入阶段进行分区级早期冲突检测，提前发现分区级冲突。

## 配置方法

### Spark 配置

```scala
import org.apache.hudi.config.HoodieWriteConfig
import org.apache.hudi.common.model.WriteConcurrencyMode
import org.apache.hudi.config.HoodieLockConfig

val hudiOptions = Map(
  "hoodie.write.concurrency.mode" -> "optimistic_concurrency_control",
  "hoodie.write.lock.provider" -> "org.apache.hudi.client.transaction.lock.ZookeeperBasedLockProvider",
  "hoodie.write.lock.conflict.resolution.strategy" -> "org.apache.hudi.client.transaction.PartitionBasedConcurrentWritesConflictResolutionStrategy",
  
  // 可选：启用早期冲突检测（推荐）
  "hoodie.write.concurrency.early.conflict.detection.enable" -> "true",
  "hoodie.write.concurrency.early.conflict.detection.strategy" -> "org.apache.hudi.table.marker.PartitionBasedDirectMarkerDetectionStrategy",
  
  // 其他必要配置
  "hoodie.datasource.write.table.type" -> "COPY_ON_WRITE",  // 或 MERGE_ON_READ
  "hoodie.datasource.write.operation" -> "upsert",
  "hoodie.datasource.write.recordkey.field" -> "id",
  "hoodie.datasource.write.partitionpath.field" -> "partition"
)
```

### Flink 配置

```java
import org.apache.hudi.configuration.FlinkOptions;
import org.apache.hudi.common.model.WriteConcurrencyMode;
import org.apache.hudi.config.HoodieLockConfig;

Configuration conf = new Configuration();
conf.setString(FlinkOptions.WRITE_CONCURRENCY_MODE.key(), WriteConcurrencyMode.OPTIMISTIC_CONCURRENCY_CONTROL.name());
conf.setString(FlinkOptions.LOCK_PROVIDER_CLASS_NAME.key(), "org.apache.hudi.client.transaction.lock.ZookeeperBasedLockProvider");
conf.setString(HoodieLockConfig.WRITE_CONFLICT_RESOLUTION_STRATEGY_CLASS_NAME.key(), 
    "org.apache.hudi.client.transaction.PartitionBasedConcurrentWritesConflictResolutionStrategy");

// 可选：启用早期冲突检测（推荐）
conf.setString(FlinkOptions.EARLY_CONFLICT_DETECTION_ENABLE.key(), "true");
conf.setString(FlinkOptions.EARLY_CONFLICT_DETECTION_STRATEGY.key(), 
    "org.apache.hudi.table.marker.PartitionBasedDirectMarkerDetectionStrategy");

// 其他必要配置
conf.setString(FlinkOptions.TABLE_TYPE.key(), "COPY_ON_WRITE");  // 或 MERGE_ON_READ
conf.setString(FlinkOptions.OPERATION.key(), "upsert");
conf.setString(FlinkOptions.RECORD_KEY_FIELD.key(), "id");
conf.setString(FlinkOptions.PARTITION_PATH_FIELD.key(), "partition");
```

### 使用示例

#### Spark 示例

```scala
import org.apache.spark.sql.SparkSession
import org.apache.hudi.DataSourceWriteOptions
import org.apache.hudi.config.HoodieWriteConfig
import org.apache.hudi.config.HoodieLockConfig

val spark = SparkSession.builder()
  .appName("Hudi Partition-Based Concurrent Write")
  .config("spark.serializer", "org.apache.spark.serializer.KryoSerializer")
  .getOrCreate()

// 准备数据（只写入特定分区）
val data = spark.createDataFrame(Seq(
  (1, "value1", "partition_a"),
  (2, "value2", "partition_a")
)).toDF("id", "value", "partition")

// 配置分区级并发控制
val hudiOptions = Map(
  "hoodie.write.concurrency.mode" -> "optimistic_concurrency_control",
  "hoodie.write.lock.provider" -> "org.apache.hudi.client.transaction.lock.ZookeeperBasedLockProvider",
  "hoodie.write.lock.conflict.resolution.strategy" -> "org.apache.hudi.client.transaction.PartitionBasedConcurrentWritesConflictResolutionStrategy",
  "hoodie.datasource.write.table.type" -> "COPY_ON_WRITE",
  "hoodie.datasource.write.operation" -> "upsert",
  "hoodie.datasource.write.recordkey.field" -> "id",
  "hoodie.datasource.write.partitionpath.field" -> "partition",
  "hoodie.datasource.write.table.name" -> "test_table",
  "hoodie.datasource.write.path" -> "/path/to/hudi/table"
)

// 写入数据
data.write
  .format("hudi")
  .options(hudiOptions)
  .mode("append")
  .save("/path/to/hudi/table")
```

#### Flink 示例

```java
import org.apache.flink.table.api.EnvironmentSettings;
import org.apache.flink.table.api.TableEnvironment;
import org.apache.hudi.configuration.FlinkOptions;
import org.apache.hudi.common.model.WriteConcurrencyMode;
import org.apache.hudi.config.HoodieLockConfig;

EnvironmentSettings settings = EnvironmentSettings.newInstance().build();
TableEnvironment tEnv = TableEnvironment.create(settings);

// 创建 Hudi 表
tEnv.executeSql(
  "CREATE TABLE hudi_table (\n" +
  "  id BIGINT,\n" +
  "  value STRING,\n" +
  "  partition STRING,\n" +
  "  PRIMARY KEY (id) NOT ENFORCED\n" +
  ") PARTITIONED BY (partition)\n" +
  "WITH (\n" +
  "  'connector' = 'hudi',\n" +
  "  'path' = '/path/to/hudi/table',\n" +
  "  'table.type' = 'COPY_ON_WRITE',\n" +
  "  'write.operation' = 'upsert',\n" +
  "  'write.concurrency.mode' = 'optimistic_concurrency_control',\n" +
  "  'write.lock.provider' = 'org.apache.hudi.client.transaction.lock.ZookeeperBasedLockProvider',\n" +
  "  'write.lock.conflict.resolution.strategy' = 'org.apache.hudi.client.transaction.PartitionBasedConcurrentWritesConflictResolutionStrategy'\n" +
  ")"
);

// 插入数据（只写入特定分区）
tEnv.executeSql(
  "INSERT INTO hudi_table VALUES " +
  "(1, 'value1', 'partition_a'), " +
  "(2, 'value2', 'partition_a')"
);
```

## 工作原理

### 1. 分区级冲突检测

传统的文件组级冲突检测：
```java
// 检查 (分区路径, 文件ID) 对的交集
Set<Pair<String, String>> intersection = partitionAndFileIdsSet1 ∩ partitionAndFileIdsSet2;
if (!intersection.isEmpty()) {
  // 冲突
}
```

分区级冲突检测：
```java
// 只检查分区路径的交集
Set<String> partitionIntersection = partitions1 ∩ partitions2;
if (!partitionIntersection.isEmpty()) {
  // 冲突
}
```

### 2. 并发写入场景

**场景1：不同分区，无冲突**
- Flink 写入 `partition_a`
- Spark 写入 `partition_b`
- ✅ 无冲突，可以并发执行

**场景2：相同分区，有冲突**
- Flink 写入 `partition_a`
- Spark 写入 `partition_a`
- ❌ 检测到冲突，其中一个写入会被中止

**场景3：部分重叠分区，有冲突**
- Flink 写入 `partition_a` 和 `partition_b`
- Spark 写入 `partition_b` 和 `partition_c`
- ❌ 检测到冲突（`partition_b` 重叠），其中一个写入会被中止

## 注意事项

### 1. 锁提供者配置

分区级并发控制仍然需要配置锁提供者（如 Zookeeper），用于：
- 早期冲突检测的事务保护
- 时间线操作的原子性

### 2. 分区隔离要求

- **必须确保不同写入者写入不同的分区**
- 如果两个写入者写入相同的分区，仍然会产生冲突
- 建议在应用层进行分区分配，确保分区隔离

### 3. 性能考虑

- 分区级并发控制比文件组级并发控制更宽松
- 在低冲突场景下，性能与文件组级相当
- 在高冲突场景下，分区级控制可以减少冲突检测的开销

### 4. 压缩和聚类操作

- 压缩操作仍然需要特殊处理
- 聚类操作如果涉及多个分区，可能会与写入操作产生冲突
- 建议在低峰期执行压缩和聚类操作

## 最佳实践

### 1. 分区分配策略

```scala
// 示例：基于写入者ID分配分区
def assignPartition(writerId: String, totalPartitions: Int): String = {
  val partitionIndex = writerId.hashCode % totalPartitions
  s"partition_$partitionIndex"
}
```

### 2. 监控冲突率

```scala
// 监控写入冲突率
val conflictRate = numConflicts / totalWrites
if (conflictRate > 0.1) {
  // 考虑调整分区分配策略
}
```

### 3. 启用早期冲突检测

```scala
// 推荐启用早期冲突检测，提前发现冲突
"hoodie.write.concurrency.early.conflict.detection.enable" -> "true",
"hoodie.write.concurrency.early.conflict.detection.strategy" -> 
  "org.apache.hudi.table.marker.PartitionBasedDirectMarkerDetectionStrategy"
```

## 故障排查

### 问题1：仍然检测到冲突

**可能原因**：
- 两个写入者写入了相同的分区
- 配置未正确应用

**解决方案**：
1. 检查写入的分区路径，确保不同写入者写入不同分区
2. 验证配置是否正确设置
3. 查看日志确认使用的冲突解决策略

### 问题2：早期冲突检测未生效

**可能原因**：
- 早期冲突检测策略配置错误
- Marker 类型不匹配

**解决方案**：
1. 确认 Marker 类型为 `DIRECT`
2. 验证早期冲突检测策略类名正确
3. 检查日志中的冲突检测信息

## 总结

分区级并发控制机制允许 Flink 和 Spark 同时并发写入不同的分区，提高了系统的并发性能。通过合理配置和分区分配策略，可以实现高效的并发写入。

---

**相关文档**：
- [Hudi OCC 并发机制详细分析](./Hudi_OCC_并发机制详细分析.md)
- [Hudi 官方文档](https://hudi.apache.org/docs/concurrency_control/)
