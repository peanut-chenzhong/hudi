# 分区级并发控制单元测试说明

## 测试文件

### 1. 提交时冲突检测策略测试

**文件**: `hudi-client/hudi-client-common/src/test/java/org/apache/hudi/client/transaction/TestPartitionBasedConcurrentWritesConflictResolutionStrategy.java`

**测试用例**:

| 测试方法 | 描述 | 预期结果 |
|---------|------|---------|
| `testNoConcurrentWrites` | 无并发写入场景 | 无冲突 |
| `testConcurrentWritesToDifferentPartitions` | 两个写入者写入不同分区 | 无冲突 |
| `testConcurrentWritesToSamePartition` | 两个写入者写入相同分区 | 有冲突，抛出异常 |
| `testConcurrentWritesWithOverlappingPartitions` | 两个写入者有重叠分区 | 有冲突，抛出异常 |
| `testConcurrentWritesToCompletelyDifferentPartitions` | 写入完全不同的分区 | 无冲突 |
| `testFlinkAndSparkWritingToDifferentPartitions` | Flink和Spark写入不同分区 | 无冲突 |
| `testComparisonWithFileGroupStrategy` | 与文件组级策略对比 | 分区级策略更宽松 |
| `testIsPreCommitRequired` | 测试预提交要求 | 返回 false |

### 2. 早期冲突检测策略测试

**文件**: `hudi-client/hudi-client-common/src/test/java/org/apache/hudi/table/marker/TestPartitionBasedDirectMarkerDetectionStrategy.java`

**测试用例**:

| 测试方法 | 描述 | 预期结果 |
|---------|------|---------|
| `testNoConflictWhenMarkersInDifferentPartitions` | Marker在不同分区 | 无冲突 |
| `testConflictWhenMarkersInSamePartition` | Marker在相同分区 | 有冲突 |
| `testResolveMarkerConflictThrowsException` | 冲突解决抛出异常 | 抛出 HoodieEarlyConflictDetectionException |
| `testFlinkAndSparkWriteToDifferentPartitions` | Flink和Spark写入不同分区 | 无冲突 |
| `testMultipleInstantsWithDifferentPartitions` | 多个Instant写入不同分区 | 无冲突 |
| `testDetectAndResolveConflictIfNecessaryNoConflict` | 无冲突时的检测 | 不抛出异常 |
| `testDetectAndResolveConflictIfNecessaryWithConflict` | 有冲突时的检测 | 抛出异常 |

## 运行测试

### 运行单个测试类

```bash
# 运行提交时冲突检测策略测试
mvn test -pl hudi-client/hudi-client-common -Dtest=TestPartitionBasedConcurrentWritesConflictResolutionStrategy

# 运行早期冲突检测策略测试
mvn test -pl hudi-client/hudi-client-common -Dtest=TestPartitionBasedDirectMarkerDetectionStrategy
```

### 运行单个测试方法

```bash
# 运行特定测试方法
mvn test -pl hudi-client/hudi-client-common -Dtest=TestPartitionBasedConcurrentWritesConflictResolutionStrategy#testConcurrentWritesToDifferentPartitions
```

### 运行所有相关测试

```bash
# 运行所有分区级并发控制相关测试
mvn test -pl hudi-client/hudi-client-common -Dtest=*PartitionBased*
```

## 测试覆盖场景

### 场景1: 不同分区并发写入（核心场景）

```
Writer 1 (Flink)          Writer 2 (Spark)
     |                          |
  写入分区 A                   写入分区 B
     |                          |
  创建 Marker A               创建 Marker B
     |                          |
  冲突检测 ───────────────────冲突检测
     |      无交集              |
  ✅ 继续写入               ✅ 继续写入
     |                          |
  提交成功                    提交成功
```

### 场景2: 相同分区并发写入（冲突场景）

```
Writer 1                  Writer 2
     |                          |
  写入分区 A                   写入分区 A
     |                          |
  创建 Marker A               创建 Marker A
     |                          |
  冲突检测 ───────────────────冲突检测
     |      有交集              |
  ✅ 继续写入               ❌ 检测到冲突
     |                          |
  提交成功                    抛出异常
```

### 场景3: 部分重叠分区（冲突场景）

```
Writer 1                  Writer 2
     |                          |
  写入分区 A, B                写入分区 B, C
     |                          |
  冲突检测 ───────────────────冲突检测
     |    分区B交集             |
  ✅ 继续                   ❌ 检测到冲突
```

## 与原有测试的对比

| 测试类 | 冲突检测级别 | 不同分区相同文件ID |
|-------|------------|------------------|
| `TestSimpleConcurrentFileWritesConflictResolutionStrategy` | 文件组级 (partition, fileId) | 可能冲突 |
| `TestPartitionBasedConcurrentWritesConflictResolutionStrategy` | 分区级 (partition) | 无冲突 |

## 关键断言

```java
// 不同分区写入 - 无冲突
assertFalse(strategy.hasConflict(flinkOperation, sparkOperation),
    "Flink and Spark should NOT conflict when writing to different partitions");

// 相同分区写入 - 有冲突
assertTrue(strategy.hasConflict(thisCommitOperation, thatCommitOperation),
    "Should have conflict when writing to the same partition");

// 冲突解决抛出异常
assertThrows(HoodieWriteConflictException.class, () -> {
    strategy.resolveConflict(null, thisCommitOperation, thatCommitOperation);
});
```

## 注意事项

1. **测试环境**: 测试使用 `HoodieCommonTestHarness` 提供的测试基础设施
2. **Marker 文件**: 早期冲突检测测试需要模拟 marker 文件和心跳文件
3. **时间线**: 测试中使用 `HoodieActiveTimeline.createNewInstantTime()` 生成唯一时间戳
4. **清理**: 测试完成后会自动清理测试目录

---

**相关源文件**:
- `PartitionBasedConcurrentWritesConflictResolutionStrategy.java`
- `PartitionBasedDirectMarkerDetectionStrategy.java`
